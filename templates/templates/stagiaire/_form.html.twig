{{ form_start(form_stagiaire) }}
<div class="form-group">
	{{ form_row(form_stagiaire.cin, {
    'label': 'CIN *',
    'required': true,
    'attr': {'placeholder': 'Votre CIN'}
  }) }}
</div>
<div class="form-group">
	{{ form_row(form_stagiaire.nom, {
    'label': 'Nom *',
    'required': true,
    'attr': {'placeholder': 'Votre nom'}
  }) }}
</div>
<div class="form-group">
	{{ form_row(form_stagiaire.prenom, {
    'label': 'Prénom *',
    'required': true,
    'attr': {'placeholder': 'Votre prénom'}
  }) }}
</div>
<div class="form-group">
	{{ form_row(form_stagiaire.mail, {
    'label': 'Email',
    'attr': {'placeholder': 'Votre adresse email'}
  }) }}
</div>
<div class="form-group">
	{{ form_row(form_stagiaire.tel, {
    'label': 'Téléphone',
    'attr': {'placeholder': 'Votre numéro de téléphone'}
  }) }}
</div>

<input type="submit" value="{{ valeurBtnSubmit|default('Ajouter le stagiaire') }}" class="btn btn-primary">
{{ form_end(form_stagiaire) }}
