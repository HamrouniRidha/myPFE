{{ form_start(form_encadrant) }}
<div class="form-group">
	{{ form_row(form_encadrant.matricule, {
    'label': 'Matricule *',
    'required': true,
    'attr': {'placeholder': 'Encadrant'}
  }) }}
</div>
<div class="form-group">
	{{ form_row(form_encadrant.nom, {
    'label': 'Nom *',
    'required': true,
    'attr': {'placeholder': 'Nom'}
  }) }}
</div>
<div class="form-group">
	{{ form_row(form_encadrant.prenom, {
    'label': 'Prénom *',
    'required': true,
    'attr': {'placeholder': 'Prénom'}
  }) }}
</div>

<input type="submit" value="{{ valeurBtnSubmit|default('Ajouter ') }}" class="btn btn-primary">
{{ form_end(form_encadrant) }}
